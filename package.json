{
  "name": "gsap-anims-dxd",
  "type": "module",
  "version": "1.0.0",
  "description": "A lightweight, attribute-driven animation system built on GSAP and CSS for Webflow projects or various front end projects",
  "scripts": {
    "build": "bun gulp build",
    "clean": "bun gulp clean",
    "dev": "bun gulp watch",
    "start": "bun gulp watch"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "del": "^8.0.0",
    "gulp": "^5.0.0",
    "gulp-clean-css": "^4.3.0",
    "gulp-flatten": "^0.4.0",
    "gulp-if": "^3.0.0",
    "gulp-rename": "^2.0.0",
    "gulp-terser": "^2.1.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
